name: Deploy to Cloudflare Pages
on:
  push:
    branches: [ main ]

permissions:
  contents: read

env:
  CF_PAGES_PROJECT: blog
  BUILD_OUT: public

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Sass
        run: npm install --global sass

      - name: Build static site
        run: |
          cargo run --release

      - name: Compile SCSS
        run: |
          sass content/styles:public/css --no-source-map
      
      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4.45.0"

          preCommands: |
            set -eux
            mkdir -p "${{ env.BUILD_OUT }}/fonts"
            wrangler r2 object get --remote my-fonts/sans.zip --file "${{ env.BUILD_OUT }}/sans.zip"
            unzip -o "${{ env.BUILD_OUT }}/sans.zip" -d "${{ env.BUILD_OUT }}/fonts"
            rm -f "${{ env.BUILD_OUT }}/sans.zip"

          command: >
            pages deploy ${{ env.BUILD_OUT }}
            --project-name=${{ env.CF_PAGES_PROJECT }}
            --branch=${{ github.event_name == 'pull_request' && github.head_ref || github.ref_name }}
